# ---------------------------------------------------------------------- #
# 1. DEPENDENCIES AND BUILD ARTIFACTS
# ---------------------------------------------------------------------- #

# Node.js dependencies
/node_modules

# Vite build output directories
/dist
/dist-ssr
/coverage

# Vite-specific cache and configuration files
.vite/
.vitepress/cache

# Vite environment variables file
# Keeps local environment secrets out of version control
.env.local
.env.*.local
*.local

# Bundlers and tool-specific build files
# Next/Nuxt/SvelteKit/etc. often use these, useful to keep them blocked
.nuxt/
.vercel/
.next/
out/


# ---------------------------------------------------------------------- #
# 2. LOGS AND LOCK FILES
# ---------------------------------------------------------------------- #

# Logs
logs
*.log

# Dependency lock files that should sometimes be ignored (but often tracked)
# NOTE: It's usually better to track package-lock.json/yarn.lock/pnpm-lock.yaml
# in final projects, but including them here if you prefer to only track package.json
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*


# ---------------------------------------------------------------------- #
# 3. EDITOR / IDE CONFIGURATION AND CACHE
# ---------------------------------------------------------------------- #

# VS Code
# Ignores all contents of the .vscode folder, EXCEPT extensions.json
# which is useful for recommending project extensions.
.vscode/*
!.vscode/extensions.json

# IntelliJ / WebStorm (etc.)
.idea
*.iml
*.iws
*.ipr

# macOS system files
.DS_Store

# Windows/Visual Studio files
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Other temporary/system files
temp
tmp
